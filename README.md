# esg-summary-generator

### Dependency

- Use `esg_toolkit` in this project

```.toml
[tool.poetry.dependencies]
python = "^3.11"
esg-toolkit = {git = "https://github.com/ESGTrans/esg_toolkit.git"}
```
